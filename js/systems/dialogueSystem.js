export class DialogueSystem {
    constructor(gameState) {
        this.gameState = gameState;
    }

    startDialogue(dialogueId) {
        // 대화 시작 로직
    }

    makeChoice(choiceId) {
        // 선택지 처리 로직
    }
} 